{
    email {$ACME_EMAIL:admin@example.com}
}

{$SITE_ADDRESS::80} {
    reverse_proxy aviation-core:8501

    log {
        output file /var/log/caddy/access.log
    }

    # Enable compression for faster dashboard loading
    encode gzip zstd
}
